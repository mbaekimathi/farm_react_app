{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup": "node -e \"require('./config/setupDatabase').setupDatabase().then(() => process.exit(0)).catch(() => process.exit(1))\"",
    "build": "echo 'Backend build completed'",
    "prod": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.14.1"
  }
}
